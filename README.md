# ${{ values.name }}

Laravel application skeleton template for Backstage scaffolding.

## Install

```bash
composer install
cp .env.example .env
php artisan key:generate
```

## Run locally

```bash
php artisan serve
```

## Run tests

```bash
php artisan test
# or
vendor/bin/phpunit
```
